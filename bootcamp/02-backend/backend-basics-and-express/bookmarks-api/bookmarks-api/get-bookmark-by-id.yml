info:
  name: Get bookmark by ID
  type: http
  seq: 2

http:
  method: GET
  url: '{{base_url}}/bookmarks/4'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
