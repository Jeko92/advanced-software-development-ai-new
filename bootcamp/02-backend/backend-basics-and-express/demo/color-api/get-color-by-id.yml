info:
  name: Get Color By ID
  type: http
  seq: 3

http:
  method: GET
  url: '{{base_url}}/colors/:id'
  params:
    - name: id
      value: '1'
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
