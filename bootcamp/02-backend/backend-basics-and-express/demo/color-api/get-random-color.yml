info:
  name: Get Random Color
  type: http
  seq: 5

http:
  method: GET
  url: '{{base_url}}/colors/random'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
