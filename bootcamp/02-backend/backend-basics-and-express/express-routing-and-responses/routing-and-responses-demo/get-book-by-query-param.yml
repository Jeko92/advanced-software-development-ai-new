info:
  name: get-book-by-query-param
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/books?author=George
  params:
    - name: author
      value: George
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
