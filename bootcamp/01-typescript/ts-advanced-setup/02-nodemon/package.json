{
  "name": "@bootcamp/nodemon-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal demo for TypeScript Advanced Setup: nodemon",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "nodemon",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "devDependencies": {
    "@bootcamp/eslint-config": "workspace:*",
    "@bootcamp/prettier-config": "workspace:*",
    "@types/node": "^22.0.0",
    "nodemon": "^3.1.14",
    "typescript": "^5.6.0"
  },
  "prettier": "@bootcamp/prettier-config"
}
